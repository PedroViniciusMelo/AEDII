Para compilar, navegue para uma pasta e use o comando:

gcc *.c -o bst

Para usar um arquivo de teste, use o comando abaixo, alterando entre os arquivos que quer usar.
./bst < testes/1.in > teste.out

diff testes/1.out testes/teste.out